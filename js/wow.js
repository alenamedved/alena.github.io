

var wow = new WOW ({
	offset:       75,          // расстояние до элемента при запуске анимации (по умолчанию 0)
	mobile:       false,       // триггерные анимации на мобильных устройствах (чтобы включить - true)
});
wow.init();

